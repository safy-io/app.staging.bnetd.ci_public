apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /ejbca/publicweb/crls/search.cgi
  name: ejbca-root-crl-ingress
spec:
  rules:
    - host: crl.bpsusine.com
      http:
        paths:
          - path: /safy-root-ca.crl
            pathType: Exact
            backend:
              service:
                name: ejbca-service
                port:
                  name: proxied-https
